# 盾构隧道预制管片密封拼环与同步注浆准入闭环

## 项目目标

建设一个面向城市轨道交通盾构隧道的质量闭环服务，将预制管片从密封槽交接、密封垫裁切粘贴、入环拼装、螺栓分级紧固、接缝复测、分舱检漏、异常拆换复验直至同步注浆准入纳入同一条可恢复证据链。系统以“整环—管片—接缝—密封分段”的只追加闭合环图为核心，采用 SQLite 事务日志与快照实现真实持久化和进程重启恢复；所有写操作携带操作号、任务代次和逻辑时间，使用稳定错误码、确定排序、整数定点运算及原子事务保证并发结果可复现。实现规模限定为约 20—28 个生产 Go 文件、2000—3000 行有效生产 Go 代码，目标约 2500 行，分布于不少于 4 个有业务意义的包；前端提供可实际操作的整环看板、材料账、工序证据、检漏轨迹和终局裁定页面，并通过确定性构建产物由 Go 服务托管。

## 端到端业务流程

1. 任务锁定：质量负责人选择区间、环号和规则摘要，提交环型、任务代次、管片全集及圆心角、类型与楔形方向、纵缝和环缝配对、整数毫米密封槽与螺栓孔位、密封垫和胶粘剂批次、工序程序、拼装顺序、紧固级次、检漏分舱邻接表及全部阈值；服务校验区间和环型兼容性、规则摘要新鲜度、身份唯一性、整数尺寸、角度总和及闭环唯一性后生成不可变锁定快照。
2. 材料与槽体准备：操作员在有效清槽工位租约内记录清洁、干燥和槽体测量仪证据，再以一个原子操作领取唯一密封垫原条、按整数毫米登记有效裁段、接头搭接、试样、余料和损耗，并领取胶粘剂代次、按整数毫克预留涂布、留样、回收和损耗；身份绑定、两类材料账和资源租约同时成功或同时回滚。
3. 粘贴与养护：依次完成裁切、接头闭合、角部缓冲材料安装、分段涂胶、粘贴和滚压养护；粘贴证据必须从锁定起点形成连续分段前缀，校验接头位置、伸长率、搭接率、单位长度涂胶量、胶粘剂开放时限、扭转和槽内偏移，禁止跳段、旧代次、重复绑定或过期涂胶台与滚压设备租约推进状态。
4. 拼装与紧固：在拼装机抓取位租约内按锁定顺序将合格管片定位入环，记录楔形方向和拼装位置；随后在扭矩工具租约内逐级记录螺栓预紧证据，并用激光错台仪复测接缝张开量、错台和密封垫压缩率。只有全部管片、纵缝和环缝形成唯一闭环且粘贴、养护和紧固前缀完整，整环才可进入检漏。
5. 分舱检漏与拆换复验：按锁定压差程序和逻辑时间向分舱写入定点压差读数，校验时间轴、试验压差积分与衰减率；发现接头开裂、空段、扭转、槽内偏移、错台、预紧不足或泄漏异常时，依据闭合环邻接、共享密封垫原条、胶粘剂代次和相邻受压分舱一次性生成唯一拆换复验集合，并创建新代次重新覆盖受影响接缝，永久保留旧材料去向、拼装位置及检漏轨迹。
6. 终局裁定：两个不同且具备资格的人员分别针对当前代次复核材料守恒、拓扑、工序前缀、几何压缩、紧固、检漏和复验结论；满足全部条件时生成唯一同步注浆准入凭据。准入、结构隔离和取消共用单写终局屏障，并发竞争只能提交一个不可逆终局，其他请求获得稳定冲突码。

## 核心组件与职责

1. 管片构造与密封材料规则目录：管理区间适用环型、管片类型、楔形方向、圆心角整数规则、密封槽与孔位整数几何、接缝模板、材料规格、粘贴紧固程序、检漏分舱及阈值版本，并产生可锁定的规则摘要。
2. 整环任务及接缝闭合聚合：保存只追加的整环—管片—接缝—密封分段图、不可变任务快照、管片位置、纵缝与环缝配对和代次；负责缺边、多重配对、闭合环不唯一及旧代次写入校验。
3. 材料守恒与资源租约管理器：以事务账本维护密封垫整数毫米和胶粘剂整数毫克守恒，实施密封垫单槽绑定；为清槽工位、涂胶台、滚压设备、拼装机抓取位、扭矩工具和检漏分舱提供基于逻辑时间的限时互斥租约、幂等获取与重启后过期判定。
4. 粘贴拼装及紧固证据记录器：执行清洁至分级紧固的依赖状态机，维护连续粘贴和紧固前缀，记录角部缓冲、开放时限、养护、入环定位、接缝几何及设备调用结果，并拒绝任何不能合法推进的证据。
5. 水密复验与终局仲裁器：保存分舱压差时间轴，执行整数定点积分和衰减判定，确定性传播异常拆换集合、隔离新旧复验代次，组织双人复核，并通过单写屏障生成准入、结构隔离或取消终局。
6. Go HTTP API 与前端：提供 JSON API、稳定错误信封、操作幂等接口和事件查询；托管一个真实单页看板，支持锁定任务、领取材料、获取租约、录入工序与检漏证据、查看闭环图和提交双人终局复核。前端包含 package.json、提交的锁文件以及固定依赖版本的确定性构建脚本。

## 领域规则与不变量

1. 锁定快照固定区间、环号、环型、任务代次、规则摘要、管片全集、圆心角、类型和楔形方向、接缝配对、槽体及孔位几何、材料批次、角部缓冲材料、粘贴起点与程序、拼装顺序、紧固级次、检漏分舱邻接表和阈值；锁定后不得就地修改，只能显式创建新任务代次。
2. 每个管片身份在任务中唯一，每个相邻边只能参与一次接缝配对；全部管片圆心角之和必须等于公开环型规则值，所有相邻管片构成唯一简单闭环，纵缝与前后环缝均完整后才允许检漏。槽宽、槽深、转角、接头位置和孔位均为非负整数毫米，并拒绝退化值以及任何乘加溢出。
3. 每条密封垫身份最多绑定一个管片槽。每根原条满足“原条长度＝有效裁段＋接头搭接＋试样＋余料＋合理损耗”，每次胶粘剂领用满足“领用质量＝有效涂布＋留样＋回收＋损耗”；所有量分别使用整数毫米和整数毫克，负值、重复归类或不平衡事务均拒绝。
4. 清洁、干燥、裁切、接头闭合、分段涂胶、粘贴、滚压养护、入环定位和分级紧固严格按锁定依赖推进。密封分段按锁定序号从起点写成连续前缀，只有下一段可被接受；开放时限含起点、不含过期终点，逻辑时间不得回退。
5. 伸长率、搭接率、单位长度涂胶量、接缝张开量、错台、压缩率、预紧偏差、压差积分和衰减率统一采用有符号 64 位整数定点值及公开的半远离零舍入规则；运算前检查符号、除零、时间间隔和乘加溢出，边界包含关系由锁定规则明确。
6. 六类资源实行单持有者限时租约；同一资源在同一逻辑时刻只能有一个有效持有者，续租必须由当前持有者执行。租约到期后可被竞争者获取，但旧持有者迟到提交无效。密封垫领取、材料扣减、身份绑定和必要租约获取必须在同一数据库事务内原子完成。
7. 设备拒绝、断连、超时或格式错误只能追加一个含设备类型、调用号、逻辑时间、确定重试序号和故障码的待重试记录，不生成测量值且不推进业务状态；超过锁定重试上限后转为异常证据。相同操作号及相同规范化内容重试返回原结果，内容不同则返回幂等冲突。
8. 异常复验集合按闭合环相邻接缝、共享密封垫原条、胶粘剂代次和受压分舱传递闭包一次性生成并排序去重；新代次生效后，旧代次回执只追加审计记录。终局要求两名不同合格人员独立复核，且准入、结构隔离、取消只能由单写屏障提交其一。

## 数据模型与持久化

1. RuleSnapshot：规则摘要、适用区间和环型、整数圆心角总和、管片模板、楔形约束、槽体和孔位几何、接缝模板、材料及工序规则、分舱邻接和定点阈值。
2. RingTask、Segment、Joint、SealSection：以区间、环号、任务代次为聚合键；保存管片序号和位置、纵缝或环缝类型、唯一配对边、密封分段序号及只追加状态事件，构成可重放闭合环图。
3. GasketBar、GasketAllocation、AdhesiveIssue、MaterialLedgerEntry：记录批次、单条身份、槽绑定、原长、有效裁段、搭接、试样、余料、损耗以及胶粘剂领用、有效涂布、留样、回收和损耗；账目行不可修改，只能以补偿行和新代次追加。
4. ResourceLease、OperationReceipt、DeviceAttempt：记录资源类型和身份、持有者、起止逻辑时间、操作号、规范化内容摘要、已提交结果，以及设备调用的重试序号、故障码和可选有效读数。
5. ProcessEvidence、BoltStageEvidence、GeometryEvidence：保存清槽、干燥、接头、涂胶、粘贴、滚压、养护、入环、分级紧固的依赖位置，连续前缀长度，以及整数测量值、仪器身份、逻辑时间和任务代次。
6. PressureTrace、RetestCase、Review、TerminalDecision：保存分舱压差点和积分结果、异常来源与确定性传播集合、新旧代次关联、复核人资格快照以及唯一终局类型和准入凭据摘要。
7. Persistence：SQLite 使用外键、唯一索引和即时写事务实现并发原子性；事件表保存完整只追加事实，派生快照和前缀可在启动时校验并重建，未提交事务在崩溃恢复时不产生可见材料扣减、租约、绑定、证据或凭据。

## 公开接口

1. POST /api/rings：校验并锁定整环任务；GET /api/rings/{section}/{ring}/graph 返回按管片序号、接缝类型和分段排序的当前闭环图、规则摘要及代次。
2. POST /api/rings/{id}/materials/allocate 与 POST /api/leases：携带 operation_id、expected_generation 和逻辑时间，原子完成材料领取、守恒记账、唯一绑定及资源租约；冲突响应包含稳定错误码和当前持有信息。
3. POST /api/rings/{id}/evidence：按证据类型提交清洁、裁切、接头、涂胶粘贴、滚压养护、拼装、紧固和几何记录；POST /api/device-attempts 记录脚本化设备成功或失败调用。
4. POST /api/rings/{id}/pressure-traces 与 POST /api/rings/{id}/retests：提交分舱压差时间轴、触发异常传播并创建新复验代次；GET 接口返回旧代次不可变轨迹和当前有效结论。
5. POST /api/rings/{id}/reviews 与 POST /api/rings/{id}/terminal-decisions：录入两个独立复核并竞争提交同步注浆准入、结构隔离或取消；成功返回唯一终局凭据，失败返回既有终局。
6. 所有错误使用 {code, operation_id, reasons[]} 信封；reasons 按区间、环号、管片序号、接缝类型、密封分段、材料代次和错误码稳定排序。前端路由 / 展示任务锁定向导、闭环图、材料守恒表、资源租约、连续工序进度、检漏曲线、复验集合和终局面板。

## 失败边界

1. 数据库写入、材料账、身份绑定、闭环边、租约、有效前缀、检漏证据和终局凭据以整项事务为边界；校验失败、唯一约束冲突、进程中断或提交错误均不得留下部分可见状态，启动时通过事件序号和快照校验恢复。
2. 区间或环型错配、摘要陈旧、管片重复、楔形方向错误、缺边、多重配对、闭环不唯一、退化或负尺寸分别返回稳定领域错误码；一次请求中的多项原因全部收集后按规定键排序。
3. 并发重复领取、材料不守恒、粘贴跳段、开放时限到期、租约过期、逻辑时间乱序、旧任务代次和操作号异内容重用在事务提交前拒绝，不改变账本、租约或工序前缀。
4. 所有定点运算在写入前做有符号范围、除零和乘加溢出检查；溢出返回固定错误码而非截断、浮点降级或部分写入。
5. 仪器失败与业务拒绝分离：设备故障只形成待重试调用，重试次数由调用号和脚本确定；没有有效读数就不能生成合格证据，超限后只能进入异常和复验路径。
6. 复验及终局使用代次比较和数据库唯一屏障抵御迟到回执与并发写入；失败竞争者读取已提交结果，不能覆盖当前结论、重复发证或撤销旧审计轨迹。

## 验收标准

1. 锁定请求完整冻结环型、楔形方向、管片全集、接缝配对、整数槽体和孔位几何、材料批次、粘贴拼装紧固程序、检漏分舱、阈值与任务代次；陈旧摘要或任一身份、方向、尺寸、角度、配对和闭环错误均以稳定排序原因拒绝。
2. 公开整数圆心角规则下，整环必须形成唯一闭环且纵缝、环缝完整；系统可确定复现缺边、多重配对、非唯一闭环、退化和负尺寸及乘加溢出，并禁止不合格整环进入检漏。
3. 密封垫长度账和胶粘剂质量账分别严格守恒；并发材料领取、单槽绑定和资源租约获取原子提交，相同操作号同内容幂等，异内容冲突或任一步失败时两类账、绑定和租约全部回滚。
4. 从清槽到分级紧固严格按锁定依赖推进，每条槽只接受连续粘贴前缀；跳段、扭转、重复绑定、错误代次、开放时限越界、过期租约、逻辑时间回退或不合格定点指标均不得写入有效证据。
5. 所有定点指标按照公开缩放、边界和半远离零规则计算，并确定拒绝负时间、除零和溢出；设备拒绝、断连、超时和格式错误只产生可重试调用，不能伪造读数或推进任务。
6. 异常能够依据闭环邻接、共享原条、胶粘剂代次和相邻受压分舱一次性产生唯一有序拆换集合；复验使用新代次覆盖受影响接缝，旧代次迟到回执不改变当前结论且历史材料去向、位置和压力轨迹可查询。
7. 仅当材料、拓扑、粘贴紧固前缀、几何压缩、压力时间轴和全部复验合格，并由两个不同合格人员复核后才生成唯一同步注浆准入凭据；准入、隔离和取消并发时数据库中恰有一个终局。
8. 仓库包含可编译运行的 Go 后端和真实连接 API 的前端，生产 Go 代码为至少 20 个文件、至少 4 个业务包、约 2000—3000 有效行且目标约 2500 行；前端含 package.json、锁文件和固定版本确定性构建。公开测试不少于 4 个测试文件和 12 个独立用例，全部使用临时数据库、逻辑时钟、脚本设备和同步屏障，无网络、墙钟或随机顺序依赖。

## 确定性测试场景

1. 规则与拓扑测试：虚构区间“澄江路—望塔站”、固定管片圆心角和槽体尺寸，验证合法唯一闭环；分别断言环型错配、楔形错误、重复管片、角度和错误、缺边、多重配对、非唯一闭环、退化几何及溢出错误码和原因排序。
2. 材料事务测试：使用公开整数毫米裁切表和整数毫克胶量表验证双账守恒；以同步屏障让两个协程争领同一密封垫，断言仅一方成功，并验证胶量失衡会回滚垫条扣减、槽绑定和租约。
3. 租约与幂等测试：两个操作者同时竞争涂胶台，验证逻辑到期边界、续租身份和旧持有者迟到提交；重复相同 operation_id 和内容返回原回执，改变内容返回稳定幂等冲突且状态不变。
4. 工序状态机测试：按固定逻辑时间完成清洁至滚压，断言连续粘贴前缀增长；尝试跳段、扭转、错误材料代次、重复槽绑定、开放时限终点提交和过期租约提交，均断言无前缀或材料账变化。
5. 拼装紧固与定点测试：验证锁定拼装顺序、楔形入环、分级紧固依赖、预紧偏差、错台和压缩率边界；覆盖半远离零舍入、负时间、除零以及压缩率和压力积分乘加溢出。
6. 设备故障测试：脚本化槽体测量仪、电子秤、滚压计数器、激光错台仪、扭矩采集器和压差传感器依次返回拒绝、断连、超时、格式错误及成功，断言确定重试序号、无伪造读数和成功前不推进。
7. 检漏传播测试：提交固定分舱邻接表和压差时间序列，验证衰减合格边界；制造接头开裂、空段、错台、预紧不足和相邻舱泄漏，精确断言唯一排序的拆换集合及共享原条、胶粘剂代次传播范围。
8. 复验恢复测试：创建新复验代次后提交旧代次迟到设备回执，断言当前结论不变而历史轨迹保留；在关键事务后重启服务，断言已提交事件恢复、未提交扣减和证据不可见、派生前缀可重建。
9. 终局竞争测试：先验证同一复核人重复签署、资格不足和复验未结案不能准入；再由三个协程经同步屏障并发请求准入、结构隔离和取消，精确断言只有一个终局、一个凭据至多生成且失败者读到相同既有决定。
10. HTTP 与前端契约测试：通过 httptest 完成锁定、材料、证据、检漏、复核和终局主流程，断言稳定 JSON 错误信封及排序；构建前端后验证页面资源可由 Go 服务访问，关键操作调用真实 API 而非静态演示数据。

## 组件追踪关系

1. 规则目录对应验收 1、2，并由规则与拓扑测试覆盖摘要锁定、整数几何、圆心角、楔形和唯一闭环。
2. 整环任务及接缝闭合聚合对应验收 1、2、6，覆盖只追加图、纵环缝配对、任务代次、异常邻接传播和重启重放。
3. 材料守恒与资源租约管理器对应验收 3、4，覆盖双材料账、单槽绑定、并发领取、互斥租约、幂等冲突及原子回滚。
4. 粘贴拼装及紧固证据记录器对应验收 4、5，覆盖连续分段前缀、开放时限、拼装次序、分级紧固、定点指标和设备重试。
5. 水密复验与终局仲裁器对应验收 5、6、7，覆盖压差时间轴、异常集合、新旧代次隔离、双人复核及单写终局竞争。
6. Go HTTP API 与前端对应验收 8，并通过端到端契约测试验证真实页面、稳定接口、确定错误排序、持久化启动以及前后端可重复构建。

## 独特性

本项目独特地把盾构管片密封垫的单条身份与整数长度守恒、胶粘剂整数质量守恒、管片接缝唯一闭环图、分段连续粘贴前缀、分舱压差传播复验和同步注浆单写终局组合为同一可恢复事务工作流；它不是一般施工验收表单，也不与既有梁体、锚杆、湿接缝、植筋、防水卷材或压浆封锚项目重复。
